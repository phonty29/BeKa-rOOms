    <nav class="navbar navbar-expand navbar-dark bg-dark">
        <div class="container-fluid">
          <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link exit-btn" aria-current="page" href="../chats">Exit</a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle users-btn" data-bs-toggle="dropdown" aria-expanded="false">Users: <span id="user-counter">0</span></a>
                <ul class="dropdown-menu user-list">
                </ul>
              </li>
            </ul>
            <div class="navbar-nav">
              <div class="nav-item dropdown" style="color: #fff;">
                <span class="name"><%= user.name %></span>  
                <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    <img src="/<%= user.avatar %>" class="rounded mx-auto d-block img-thumbnail avatar" alt="avatar">
                </a>
                <ul class="dropdown-menu">
                  <% if (chatIsMine) { %>
                  <li><span class="dropdown-item rename-link">Rename chat</span></li>
                  <li><span class="dropdown-item deleteChat-link">Delete chat</span></li>
                  <% } else { %>
                  <li><span class="dropdown-item logout-link">Log out</span></li>
                  <li><span class="dropdown-item delete-link">Delete account</span></li>
                  <% } %>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </nav>


